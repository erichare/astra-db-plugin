{
  "name": "astra-widgets-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Astra DB widgets MCP server: collection card, similarity view, explorer, database overview (MCP Apps UI + standalone HTML).",
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "seed": "tsx scripts/seed-demo.ts",
    "pretest": "node scripts/build.mjs --ui-only"
  },
  "dependencies": {
    "@datastax/astra-db-ts": "^2.3.0",
    "@modelcontextprotocol/ext-apps": "^1.7.5",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.2",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.11"
  }
}
