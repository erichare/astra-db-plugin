---
expect:
  name: [scratch_embeddings]
error: true
---

confirmation_required: Drops collection default_keyspace.scratch_embeddings (~3 documents). This needs the user's explicit confirmation.
Hint: Tell the user exactly what will be lost and ask them to confirm; the original request does not count. Only after they approve, call again with confirm: "scratch_embeddings".
